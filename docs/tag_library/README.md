# Tag Library Research

This directory is an isolated research artifact.

It was generated from the local SAA tag table:

`D:\Arc-AI绘画\saa-win32-x64\resources\app\data\danbooru_e621_merged_zh_cn.csv`

Current status:

- Not imported by backend runtime.
- Not used by prompt generation.
- Kept only as a possible future reference for image prompt tooling.

If the project later needs tag selectors, camera/action validation, outfit state machines,
or NSFW prompt consistency rules, revisit this directory and `scripts/analyze_danbooru_tags.py`.
